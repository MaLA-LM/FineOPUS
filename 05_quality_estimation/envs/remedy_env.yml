name: remedy_vllm092
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.12
  - pip
  
  - pytorch
  - pytorch-cuda=12.4  # Matches modern vLLM requirements
  
  # Used directly by Remedy code
  - pandas
  - scipy

  # Remedy-declared deps
  - scikit-learn>=1.6.0
  - matplotlib>=3.10.0

  - pip:
      - remedy-mt-eval==0.1.3
      - vllm==0.9.2
      - transformers<4.54.0
      - datasets>=3.1.0
      - trl>=0.12.0
      - hf-transfer>=0.1.8
      - pyarrow
      - pydantic
      - huggingface_hub>=0.30,<1.0
      - safetensors>=0.4.3
